<UserControl x:Class="ListBuoy"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <ListBox Name="BuoyElements" Background="Transparent" BorderBrush="{Binding BorderBrush}" BorderThickness="{Binding BorderThickness}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.Template>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Background="{TemplateBinding Background}">
                        <ScrollViewer Padding="{TemplateBinding Padding}">
                            <WrapPanel IsItemsHost="True" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </ListBox.Template>
        </ListBox>

        <Border Name="BorderProgress" BorderThickness="0" Visibility="Collapsed">
            <ProgressBar IsIndeterminate="True" Width="200" Height="25" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </Border>
    </Grid>
    
</UserControl>
